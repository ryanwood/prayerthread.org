<%= render :partial => "header" %>

<%= @comment.user.name %> commented on '<%= @prayer.title %>'.

"<%= truncate(@comment.body, :length => 200) %>"

<%= prayer_url( @prayer, :host => HOST, :anchor => "comment_#{@comment.id}" ) %>

<%= render :partial => "footer" %>