base: &base
  do_not_reply: no-reply@prayerthread.org

development:
  <<: *base
  domain: localhost:3000

test:
  <<: *base
  domain: test.host

production:
  <<: *base
  domain: prayerthread.heroku.com
